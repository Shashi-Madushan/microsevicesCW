server:
  port: 8091
  servlet:
    context-path: /vehicle-service

spring:
  application:
    name: vehicle-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/